unit World3D;

interface

type

  {—труктура камеры, не завис€ща€ от используемой графической библиотеки}
  TCamera3D=record
    { оординаты камеры в мировом пространстве}
    CX:integer;
    CY:integer;
    CZ:integer;
    {ѕовороты камеры в мировом пространстве}
    RX:integer;
    RY:integer;
    RZ:integer;
    {÷ель камеры - в какую точку камера смотрит}
    TX:integer;
    TY:integer;
    TZ:integer;
  end;

  {—труктура, содержаща€ параметры кисти}
  TBrush3D=record
    { оординаты кисти в мировом пространстве}
    CX:integer;
    CY:integer;
    CZ:integer;
    {–азмер (радиус от центра) кисти в мировом пространства}
    SX:integer;
    SY:integer;
    SZ:integer;
  end;

implementation

end.
